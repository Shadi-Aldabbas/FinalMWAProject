// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  // REST_API_ACTORS_BASE_URL:"http://localhost:3000/api/",
  REST_API_BASE_URL:"Base URL for the rest API",
  TOKEN_STORAGE:"the name of the key in token in the browser storege",
  PASTE_MSG_ONE:"who said paste is allowed",
  PASTE_MSG_TWO:"I am just joking prof",
  PASTE_MSG_THREE :"I actually wish that we have MWA2 I'll be the first student. \n now just paste again I don't want to waist your time. \n wish you the best and take care ",
  COPY_MSG_ONE:"copy and paste are NOT allowed",
  COPY_MSG_TWO:"Just Kidding here is the password:  56723423984734230294208438432987343872984!!!!!@#$%^&*",
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
